server:
  port: 8081   #端口号
  servlet:
    context-path: /proj    #项目访问路径
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_health?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: 123456
mybatis:
    mapper-locations: classpath:mapper/*.xml
# classpath:是resources  resources/mapper/*.xml

